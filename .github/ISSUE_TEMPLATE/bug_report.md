---
name: Bug report
about: Something in the graph, store, gates, or render pipeline isn't behaving
title: "bug(scope): short description"
labels: bug
assignees: ''
---

**Which package/area is affected?**
e.g. `packages/graph`, `packages/store`, `schema/`, `src/render`, one of the six gates

**What did you expect to happen?**

**What actually happened?**
Include the exact error, gate failure message, or unexpected output.

**Steps to reproduce**
1.
2.
3.

**Repro data (if relevant)**
If this is content- or schema-shaped (e.g. a binding round-trip failure,
a dangling reference), include the minimal document/binding that triggers it.

**Environment**
- Node version:
- `npm run doctor` output (paste relevant lines):

**Suggested level**
Level 1 (self-contained) / Level 2 (touches schema, store, or graph internals) — your best guess is fine.
